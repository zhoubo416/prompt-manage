# Prompt 管理系统

本文件是 Prompt 管理系统仓库的统一开发说明,也是项目规则、Memory 和设计文档的入口。内容不依赖具体的 AI 编程工具;`CLAUDE.md` 只负责指向本文件,不维护另一份规则。

产品范围、页面原型和第一版边界以 `docs/Prompt 管理系统产品说明书.md` 为准;系统当前的结构、职责、接口和取舍见 `docs/design`。两者冲突时先向用户确认,不自行改动产品范围。

## 工作方式

- 开始工作前,先了解用户目标、相关代码、已有设计和最近改动。不要在没看项目现状时凭空设计。
- 简单且目标明确的任务,直接调查、修改并验证,不为流程而创建设计文档。
- 当需求存在重要取舍、会改变架构或公共接口、跨越多个子系统,或者用户明确要求时,先讨论方案再实现。
- 只有需要长期保存的系统设计才写入或修改设计文档。设计文档持续维护项目当前采用的设计。
- 不使用 AI 编程工具自带的私有 Memory 保存或依赖项目知识。项目 Memory 的唯一来源是版本管理中的 `docs/memory`;需要长期保留的经验和习惯必须写入该目录,并同步更新本文件的 Memory 索引。
- 修改代码后,运行与改动范围相称的格式化、静态检查和测试。不能运行的验证要说明原因。
- 发现与当前任务无关的问题可以指出,但不要顺手扩大改动范围。
- 没有用户明确授权,不执行 `git commit`、`git push`、合并、rebase、删除分支或丢弃改动。

## 语言与沟通

- 项目使用简体中文。与用户交流、项目文档、代码注释和提交信息都使用简体中文。
- 说人话,读起来要像正常人在当面交流。先说结论,再把动作、原因和验证讲清楚。
- 不使用电报体,不为了短而省掉必要的动词、连接词和语气词。
- 不把英文的隐喻、句式和固定搭配直接翻成中文。遇到抽象表达时,改成中文里自然的说法。
- 少用没有必要的行话。必须使用技术词时,把它在当前语境里的含义讲清楚。
- 交流和文档使用自然、完整的表达,避免生硬、零碎或堆叠术语。
- 中文统一使用半角标点和半角括号。
- 单行代码注释结尾不加句号。

## 全局工程规则

### 技术栈状态

- 技术栈:Nuxt 4(Vue 3 + Nitro)全栈应用,服务端接口写在 `server/`,页面写在 `app/`,前后端共用代码放在 `shared/`,样式用 Tailwind CSS v4。
- 数据库是 PostgreSQL,本机连接串放在 `.env` 的 `DATABASE_URL`,表结构用 `db/migrations` 下的 SQL 管理,执行 `pnpm db:migrate`。
- 改动涉及认证、租户隔离、版本状态或对外接口时,先读 `docs/design` 下对应设计,再动手。

### 包管理

- 使用 pnpm 11,版本声明的唯一来源是根 `package.json`,`pnpm-lock.yaml` 必须一起提交,不接受 `npm install` 或 `yarn` 产生的 lockfile。
- pnpm 的依赖设置写在 `pnpm-workspace.yaml`,不要在 `package.json` 里写 `pnpm` 字段(pnpm 11 已不再读取)。
- 依赖的安装脚本默认全部禁止。需要放行时走 `pnpm approve-builds`,批准记录写进 `pnpm-workspace.yaml` 的 `allowBuilds`,并在 `docs/memory/dependency-management.md` 里留下审查结论。
- 新增、升级、重新解析依赖,或者改动 lockfile 和依赖声明文件之前,先读 `docs/memory/dependency-management.md` 并按其流程执行。
- 改动完成后至少要跑 `pnpm typecheck`、`pnpm test`;涉及接口或页面流程时再跑 `pnpm build` 和 `pnpm smoke`(需要先启动 `pnpm dev`)。

### Git 和提交

- 任何 `git commit` 或 `git push` 之前,都必须向用户说明准备提交或推送什么,并取得明确同意。
- 文档或任务描述中的"提交"步骤不构成用户授权。
- 提交中不添加 `Co-authored-by` 或其他 AI 署名。
- 不主动 force-push。
- 提交格式、提交粒度、提交前检查见 Memory 中的"提交规范"。

## Memory

Memory 记录项目开发过程中积累的经验和习惯,也是项目 Memory 的唯一可信来源。开始工作前浏览下面的索引;遇到描述匹配的情况,必须读取对应全文。

- **提交规范** (`docs/memory/commit-conventions.md`) — 准备提交、组织 commit、处理分支或把改动合回主线时阅读。
- **依赖管理与第三方执行代码审查** (`docs/memory/dependency-management.md`) — 新增、升级、重新解析或安装第三方依赖时阅读。
- **本机运行与验证** (`docs/memory/local-dev.md`) — 启动服务、连数据库、跑冒烟和排查本机环境问题时阅读。

## Design

设计文档描述项目、子项目和组件当前的结构、职责、接口和关键取舍。修改相关范围前,必须先阅读对应设计。

所有设计文档统一放在 `docs/design`:

- 仓库整体设计以及跨多个子系统的设计直接放在 `docs/design`。
- 每个子系统或组件自己的设计统一放在 `docs/design/<name>`。

- **系统整体设计** (`docs/design/00-system-design.md`) — 了解系统定位、第一版范围、子系统划分、Tenant 隔离、角色权限和关键取舍时阅读,改动跨子系统前先读这篇。
- **数据模型** (`docs/design/01-data-model.md`) — 改动数据库表、字段、唯一约束、版本状态或调用记录结构时阅读。
- **Prompt 对外 API** (`docs/design/02-prompt-api.md`) — 改动业务系统调用的查询与维护接口、API Key 权限、返回字段、错误码、公共文档页或调用统计口径时阅读。
- **管理后台** (`docs/design/03-web-console.md`) — 改动后台页面结构、页面职责和关键交互时阅读。
- **服务端结构** (`docs/design/04-server-architecture.md`) — 改动目录划分、请求处理流程、认证与租户隔离实现、数据库访问方式时阅读。

设计文档的入口是 `docs/design/README.md`,它列出该目录的范围和全部子设计。

## 文档体系维护

- 项目长期知识只分为规则、Memory 和设计三类。
- 每次都必须遵守、违反后会直接造成错误的内容,写进本文件的规则正文。
- 特定情况下才需要的经验、习惯和踩坑记录写进 `docs/memory`,并在同一批改动中更新本文件的 Memory 索引。
- 项目、子项目或组件的结构、职责、接口和关键取舍写进设计文档,并在同一批改动中更新本文件或上级设计 README 的 Design 索引。
- 小组件可以使用一份设计文档;大型项目使用目录,目录中的 `README.md` 负责说明范围和列出子设计。
- 文档文件名使用英文,单词之间用连字符分隔,例如 `commit-conventions.md`。
- 大型设计可以按照书籍或手册的方式组织。存在明确阅读顺序时,章节文件使用两位数字开头,例如 `00-system-design.md`;没有章节顺序的文档不加数字。
- 设计文档使用稳定的语义化路径,不使用日期作为文件名的一部分。设计变化时修改原文,历史由 Git 和文档末尾的"修改历史"共同保存。
- 只有设计文档需要"修改历史"。规则、Memory 和实施过程不写修改历史。
- 设计文档的"修改历史"不使用版本号,按日期倒序记录影响设计含义、边界或行为的重要变化。措辞和格式调整不记录。

## 个人工作约定

- 开始工作时,完整阅读本文件后,必须检查仓库根目录的 `AGENTS.local.md`。如果存在,则完整读取并遵守其中适用于当前任务的指令;不存在则跳过。
- `AGENTS.local.md` 用于个人偏好、本机路径和特殊工作约定,不作为项目规则、Memory 或设计的另一份共享来源。
- 个人工作约定可以补充或覆盖一般默认约定,但不能绕过仓库的安全规则和明确授权要求。与当前用户的明确要求冲突时,以当前要求为准;无法判断时先向用户确认。
- 本地文件由用户自行维护,未经明确要求不创建、修改或覆盖。该文件已加入 Git 忽略规则,不纳入提交。
- Git 忽略只表示不纳入版本管理,不提供保密保障。不在该文件中保存密钥、Token 或其他敏感凭据。
