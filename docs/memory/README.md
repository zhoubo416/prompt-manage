# Memory

本目录记录项目开发过程中积累的经验、习惯和踩坑记录,是项目 Memory 的唯一可信来源。每条 Memory 使用英文文件名,单词之间用连字符分隔,并在根 `AGENTS.md` 的 Memory 索引里登记。

- 新增一条经验或习惯时,写进本目录对应文件,并同步更新 `AGENTS.md` 的 Memory 索引。
- 遇到与索引描述匹配的场景时,必须读取对应全文,不要只看索引。
- Memory 不写"修改历史",历史由 Git 保存。
